[package]
name = "workspace-core"
version = "0.1.0"
description = "Core abstractions and detection mechanisms for JavaScript/TypeScript workspace management"
readme = "README.md"
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
keywords = ["workspace", "monorepo", "javascript", "typescript", "package-manager"]
categories = ["development-tools", "command-line-utilities"]

[dependencies]
# Internal crates
workspace-fs = { workspace = true }

# Error handling
snafu = { workspace = true }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Logging
log = { workspace = true }

# Directory traversal
walkdir = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
