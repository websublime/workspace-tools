# Ensure optional dependencies are always installed
# This is needed for packages like rolldown that have platform-specific bindings
optional=true

# Ensure peer dependencies are installed
auto-install-peers=true

# Hoist all packages to the root node_modules
shamefully-hoist=true
